import 'package:flutter/material.dart';

class Scrap {
  String scrapName;
  IconData icon;

  Scrap(this.scrapName, this.icon);

  getScrapName() {
    return scrapName;
  }

  collide() {
    var no = {collide: Function};
    print(no);
  }
}
